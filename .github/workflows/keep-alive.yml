name: Keep Streamlit Apps Awake

on:
  schedule:
    - cron: '0 */6 * * *'   # every 6 hours
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping People Analytics app
        run: curl -sSf https://portfolio-data-u8h8myatb3ivw5nvq5euzf.streamlit.app/ > /dev/null

      - name: Ping Behavioral Churn app
        run: curl -sSf https://portfolio-data-xd5ft4evs6zrqszqu2prwf.streamlit.app/ > /dev/null

      - name: Ping Funnel Testing app
        run: curl -sSf https://portfolio-data-gyvkkaieywnlkpbacxrr5n.streamlit.app/ > /dev/null
